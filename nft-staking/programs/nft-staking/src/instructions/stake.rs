use anchor_lang::prelude*;

use anchor_spl::{
    metadata::{
        spl_token_metadata::instructions::{
            FreezeDelegatedAccountCpi,
            Freeze DelegatedAccountCpiAccounts
        },
        MasterEditionAccoumt,
        Metadata,
        MetadataAccount,
    }
    token::{
        Mint,
        TOken,
        TokenAccount
    },
};

pub struct Stake<'info> {
    
}